<%= form_for(project) do |f| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="inputHolder">
    <tbody>
      <tr>
        <td><%= f.label :Název %></td>
        <td><%= f.text_field :title %></td>
      </tr>
      <tr>
        <td><%= f.label :Místo %></td>
        <td><%= f.text_field :place %></td>
      </tr>
      <tr>
        <td><%= f.label :Stupeň %></td>
        <td><%= f.text_field :realization %></td>
      </tr>
      <tr>
        <td><%= f.label :Investor %></td>
        <td><%= f.text_field :investor %></td>
      </tr>
      <tr>
        <td><%= f.label :Team %></td>
        <td><%= f.text_field :author %></td>
      </tr>
      <tr>
        <td><%= f.label :Pořadí %></td>
        <td><%= f.number_field :order %></td>
      </tr>
      <tr>
        <td><%= f.label :Popis %></td>
        <td><%= f.text_area :description %></td>
      </tr>
    </tbody>
  </table>
  <div class="btnWrap">
    <%= f.submit %>
  </div>
<% end %>
